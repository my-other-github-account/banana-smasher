Read-only banana-smasher sensor, no-agent mode. Output is observation only and is NOT an instruction to score, launch, kill, delete, clear gates, release claims, modify task status, or edit dispatcher orders. Only the existing default dispatcher owns decisions. Failed/missing scoring stays pending with an owner; verified metric is not automatic GREEN. Empty registry is an actionable registration defect, not SENSOR_OK. Preserve artifacts and foreign processes. Canonical contract: tools/autonomy/OPERATIONS.md.
