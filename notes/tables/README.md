# Tables

Place decision-grade quantitative tables here. Required columns depend on the comparison, but model-quality tables normally include method, exact basis, KLD, top-1, GB, packed bpw, FP baseline, instrument, sample count, status, and source receipt.
